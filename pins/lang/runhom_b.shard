;;; runhom_b — run-mode homonym fixture, module B: the SAME-NAMED helper as
;;; runhom_a with a different value. Loaded SECOND, so under flat-core run
;;; resolution (keep-first) its rh_pick_b silently called module A's helper.

(fn rh_helper () Int 2)

(fn rh_pick_b () Int (rh_helper))
