;;; proof sidecar — MACHINE-OWNED, written by tools/prove (comments are
;;; not preserved). check REPLAYS these entries; it never searches.

(proof-for
  cond_probe
  (rewrite-with (lemma cap_small) lr lhs () ((by arith (list 1 1))) refl))
(proof-for
  mine_probe
  (have (= (lt (mod n 7) 7) True)
    (rewrite-with (lemma mod_hi) lr lhs () ((steps ((compute both)) refl)) refl)
    (have (= (le 0 (mod n 7)) True)
      (rewrite-with
        (lemma mod_lo)
        lr
        lhs
        ()
        ((steps ((compute both)) refl))
        refl)
      (by arith (list 1 0 1)))))
(proof-for
  mine_pair_probe
  (have (= (le 0 (+ (mod n 7) 3)) True)
    (rewrite-with (lemma mine_probe) lr lhs () () refl)
    (have (= (lt (mod n 7) 7) True)
      (rewrite-with
        (lemma mod_hi)
        lr
        lhs
        ()
        ((steps ((compute both)) refl))
        refl)
      (have (= (lt (mod n 5) 5) True)
        (rewrite-with
          (lemma mod_hi)
          lr
          lhs
          ()
          ((steps ((compute both)) refl))
          refl)
        (have (= (le 0 (mod n 7)) True)
          (rewrite-with
            (lemma mod_lo)
            lr
            lhs
            ()
            ((steps ((compute both)) refl))
            refl)
          (have (= (le 0 (mod n 5)) True)
            (rewrite-with
              (lemma mod_lo)
              lr
              lhs
              ()
              ((steps ((compute both)) refl))
              refl)
            (by arith (list 1 0 1 0 0 1))))))))
