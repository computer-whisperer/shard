;;; proof sidecar — MACHINE-OWNED, written by tools/prove (comments are
;;; not preserved). check REPLAYS these entries; it never searches.

(proof-for
  nm_walk#0#decrease
  (steps ((rewrite (premise 0) lr lhs true ()) (simp lhs)) (by arith (list))))
(proof-for
  nm_walk#0#nonneg
  (steps ((rewrite (lemma size_sexpr_list_nonneg) lr lhs true ())) refl))
(proof-for
  nm_arms#0#decrease
  (have (= (le 0 (size_sexpr arm)) True)
    (rewrite-with (lemma size_sexpr_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 1)))))
(proof-for
  nm_arms#0#nonneg
  (steps ((rewrite (lemma size_sexpr_list_nonneg) lr lhs true ())) refl))
(proof-for
  nm_arms#1#decrease
  (have (= (le 0 (size_sexpr pat)) True)
    (rewrite-with (lemma size_sexpr_nonneg) lr lhs () () refl)
    (have (= (le 0 (size_sexpr_list rest)) True)
      (rewrite-with (lemma size_sexpr_list_nonneg) lr lhs () () refl)
      (steps
        ((rewrite (premise 0) lr lhs true ())
         (rewrite (premise 1) lr lhs true ())
         (simp lhs))
        (by arith (list 1 0 0 1 1))))))
(proof-for
  nm_arms#1#nonneg
  (steps ((rewrite (lemma size_sexpr_nonneg) lr lhs true ())) refl))
(proof-for
  nm_arms#2#decrease
  (have (= (le 0 (size_sexpr arm)) True)
    (rewrite-with (lemma size_sexpr_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 1)))))
(proof-for
  nm_arms#2#nonneg
  (steps ((rewrite (lemma size_sexpr_list_nonneg) lr lhs true ())) refl))
