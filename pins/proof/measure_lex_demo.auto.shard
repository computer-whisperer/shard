;;; proof sidecar — MACHINE-OWNED, written by tools/prove (comments are
;;; not preserved). check REPLAYS these entries; it never searches.

(proof-for f#0#decrease (by arith (list)))
(proof-for
  f#0#nonneg
  (have (= (le 0 (tsz t)) True)
    (rewrite-with (lemma tsz_nonneg) lr lhs () () refl)
    (steps ((simp lhs)) (by arith (list 1 2)))))
(proof-for
  g#0#decrease
  (have (= (le 0 (tsz r)) True)
    (rewrite-with (lemma tsz_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 2)))))
(proof-for
  g#0#nonneg
  (have (= (le 0 (tsz l)) True)
    (rewrite-with (lemma tsz_nonneg) lr lhs () () refl)
    (steps ((simp lhs)) (by arith (list 1 0 2)))))
(proof-for
  g#1#decrease
  (have (= (le 0 (tsz l)) True)
    (rewrite-with (lemma tsz_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 2)))))
(proof-for
  g#1#nonneg
  (have (= (le 0 (tsz r)) True)
    (rewrite-with (lemma tsz_nonneg) lr lhs () () refl)
    (steps ((simp lhs)) (by arith (list 1 0 2)))))
