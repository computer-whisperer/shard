;;; proof sidecar — MACHINE-OWNED, written by tools/prove (comments are
;;; not preserved). check REPLAYS these entries; it never searches.

(proof-for
  check_sequent#0#decrease
  (steps ((rewrite (premise 0) lr lhs true ()) (simp lhs)) (by arith (list))))
(proof-for
  check_sequent#0#nonneg
  (steps ((rewrite (lemma proof_size_nonneg) lr lhs true ())) refl))
(proof-for
  check_sequent#1#decrease
  (steps ((rewrite (premise 0) lr lhs true ()) (simp lhs)) (by arith (list))))
(proof-for
  check_sequent#1#nonneg
  (steps ((rewrite (lemma case_list_size_nonneg) lr lhs true ())) refl))
(proof-for
  check_sequent#2#decrease
  (steps ((rewrite (premise 0) lr lhs true ()) (simp lhs)) (by arith (list))))
(proof-for
  check_sequent#2#nonneg
  (steps ((rewrite (lemma case_list_size_nonneg) lr lhs true ())) refl))
(proof-for
  check_sequent#3#decrease
  (steps ((rewrite (premise 0) lr lhs true ()) (simp lhs)) (by arith (list))))
(proof-for
  check_sequent#3#nonneg
  (steps ((rewrite (lemma proof_size_nonneg) lr lhs true ())) refl))
(proof-for
  check_sequent#4#decrease
  (steps ((rewrite (premise 0) lr lhs true ()) (simp lhs)) (by arith (list))))
(proof-for
  check_sequent#4#nonneg
  (steps ((rewrite (lemma proof_size_nonneg) lr lhs true ())) refl))
(proof-for
  check_sequent#5#decrease
  (have (= (le 0 (proof_size rest)) True)
    (rewrite-with (lemma proof_size_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 0 0 0 0 0 1)))))
(proof-for
  check_sequent#5#nonneg
  (steps ((rewrite (lemma proof_list_size_nonneg) lr lhs true ())) refl))
(proof-for
  check_sequent#6#decrease
  (have (= (le 0 (proof_list_size premise_proofs)) True)
    (rewrite-with (lemma proof_list_size_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 0 0 0 0 0 1)))))
(proof-for
  check_sequent#6#nonneg
  (steps ((rewrite (lemma proof_size_nonneg) lr lhs true ())) refl))
(proof-for
  check_sequent#7#decrease
  (steps ((rewrite (premise 0) lr lhs true ()) (simp lhs)) (by arith (list))))
(proof-for
  check_sequent#7#nonneg
  (steps ((rewrite (lemma fincase_list_size_nonneg) lr lhs true ())) refl))
(proof-for
  check_sequent#8#decrease
  (have (= (le 0 (proof_size pr)) True)
    (rewrite-with (lemma proof_size_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 0 1)))))
(proof-for
  check_sequent#8#nonneg
  (steps ((rewrite (lemma proof_size_nonneg) lr lhs true ())) refl))
(proof-for
  check_sequent#9#decrease
  (have (= (le 0 (proof_size pe)) True)
    (rewrite-with (lemma proof_size_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 0 0 1)))))
(proof-for
  check_sequent#9#nonneg
  (steps ((rewrite (lemma proof_size_nonneg) lr lhs true ())) refl))
(proof-for
  check_sequent#10#decrease
  (steps ((rewrite (premise 0) lr lhs true ()) (simp lhs)) (by arith (list))))
(proof-for
  check_sequent#10#nonneg
  (steps ((rewrite (lemma proof_size_nonneg) lr lhs true ())) refl))
(proof-for
  check_sequent#11#decrease
  (steps ((rewrite (premise 0) lr lhs true ()) (simp lhs)) (by arith (list))))
(proof-for
  check_sequent#11#nonneg
  (steps ((rewrite (lemma proof_size_nonneg) lr lhs true ())) refl))
(proof-for
  check_sequent#12#decrease
  (steps ((rewrite (premise 0) lr lhs true ()) (simp lhs)) (by arith (list))))
(proof-for
  check_sequent#12#nonneg
  (steps ((rewrite (lemma proof_size_nonneg) lr lhs true ())) refl))
(proof-for
  check_sequent#13#decrease
  (steps ((rewrite (premise 0) lr lhs true ()) (simp lhs)) (by arith (list))))
(proof-for
  check_sequent#13#nonneg
  (steps ((rewrite (lemma proof_list_size_nonneg) lr lhs true ())) refl))
(proof-for
  check_premise_proofs#0#decrease
  (have (= (le 0 (proof_list_size pf_rest)) True)
    (rewrite-with (lemma proof_list_size_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 1)))))
(proof-for
  check_premise_proofs#0#nonneg
  (steps ((rewrite (lemma proof_size_nonneg) lr lhs true ())) refl))
(proof-for
  check_premise_proofs#1#decrease
  (have (= (le 0 (proof_size pf)) True)
    (rewrite-with (lemma proof_size_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 1)))))
(proof-for
  check_premise_proofs#1#nonneg
  (steps ((rewrite (lemma proof_list_size_nonneg) lr lhs true ())) refl))
(proof-for
  check_caseon_proofs#0#decrease
  (have (= (le 0 (proof_size pf)) True)
    (rewrite-with (lemma proof_size_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 1)))))
(proof-for
  check_caseon_proofs#0#nonneg
  (steps ((rewrite (lemma case_list_size_nonneg) lr lhs true ())) refl))
(proof-for
  check_caseon_proofs#1#decrease
  (have (= (le 0 (case_list_size rest)) True)
    (rewrite-with (lemma case_list_size_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 0 1)))))
(proof-for
  check_caseon_proofs#1#nonneg
  (steps ((rewrite (lemma proof_size_nonneg) lr lhs true ())) refl))
(proof-for
  check_caseon_proofs#2#decrease
  (have (= (le 0 (proof_size pf)) True)
    (rewrite-with (lemma proof_size_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 0 0 1)))))
(proof-for
  check_caseon_proofs#2#nonneg
  (steps ((rewrite (lemma case_list_size_nonneg) lr lhs true ())) refl))
(proof-for
  fin_check_cases#0#decrease
  (have (= (le 0 (fincase_list_size rest)) True)
    (rewrite-with (lemma fincase_list_size_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 0 1)))))
(proof-for
  fin_check_cases#0#nonneg
  (steps ((rewrite (lemma proof_size_nonneg) lr lhs true ())) refl))
(proof-for
  fin_check_cases#1#decrease
  (have (= (le 0 (proof_size sub)) True)
    (rewrite-with (lemma proof_size_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 0 0 1)))))
(proof-for
  fin_check_cases#1#nonneg
  (steps ((rewrite (lemma fincase_list_size_nonneg) lr lhs true ())) refl))
(proof-for
  check_induct_proofs#0#decrease
  (have (= (le 0 (proof_size pf)) True)
    (rewrite-with (lemma proof_size_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 1)))))
(proof-for
  check_induct_proofs#0#nonneg
  (steps ((rewrite (lemma case_list_size_nonneg) lr lhs true ())) refl))
(proof-for
  check_induct_proofs#1#decrease
  (have (= (le 0 (case_list_size rest)) True)
    (rewrite-with (lemma case_list_size_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 0 1)))))
(proof-for
  check_induct_proofs#1#nonneg
  (steps ((rewrite (lemma proof_size_nonneg) lr lhs true ())) refl))
(proof-for
  check_induct_proofs#2#decrease
  (have (= (le 0 (proof_size pf)) True)
    (rewrite-with (lemma proof_size_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 0 0 1)))))
(proof-for
  check_induct_proofs#2#nonneg
  (steps ((rewrite (lemma case_list_size_nonneg) lr lhs true ())) refl))
