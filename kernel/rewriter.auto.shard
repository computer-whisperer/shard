;;; proof sidecar — MACHINE-OWNED, written by tools/prove (comments are
;;; not preserved). check REPLAYS these entries; it never searches.

(proof-for
  rewrite_all#0#decrease
  (steps ((rewrite (premise 1) lr lhs true ()) (simp lhs)) (by arith (list))))
(proof-for
  rewrite_all#0#nonneg
  (steps ((rewrite (lemma size_list_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_all#1#decrease
  (steps ((rewrite (premise 1) lr lhs true ()) (simp lhs)) (by arith (list))))
(proof-for
  rewrite_all#1#nonneg
  (steps ((rewrite (lemma size_list_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_all#2#decrease
  (steps ((rewrite (premise 1) lr lhs true ()) (simp lhs)) (by arith (list))))
(proof-for
  rewrite_all#2#nonneg
  (have (= (le 0 (size el)) True)
    (rewrite-with (lemma size_nonneg) lr lhs () () refl)
    (have (= (le 0 (size t)) True)
      (rewrite-with (lemma size_nonneg) lr lhs () () refl)
      (have (= (le 0 (size c)) True)
        (rewrite-with (lemma size_nonneg) lr lhs () () refl)
        (steps ((simp lhs)) (by arith (list 1 0 0 1 1 1)))))))
(proof-for
  rewrite_all#3#decrease
  (have (= (le 0 (size_arms arms)) True)
    (rewrite-with (lemma size_arms_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 1) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 1)))))
(proof-for
  rewrite_all#3#nonneg
  (steps ((rewrite (lemma size_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_all#4#nonneg
  (steps ((rewrite (lemma size_arms_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_all_arms#0#decrease
  (have (= (le 0 (size_arms t)) True)
    (rewrite-with (lemma size_arms_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 1)))))
(proof-for
  rewrite_all_arms#0#nonneg
  (steps ((rewrite (lemma size_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_all_arms#1#decrease
  (have (= (le 0 (size b)) True)
    (rewrite-with (lemma size_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 1)))))
(proof-for
  rewrite_all_arms#1#nonneg
  (steps ((rewrite (lemma size_arms_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_all_arms#2#decrease
  (have (= (le 0 (size b)) True)
    (rewrite-with (lemma size_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 1)))))
(proof-for
  rewrite_all_arms#2#nonneg
  (steps ((rewrite (lemma size_arms_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_all_list#0#nonneg
  (steps ((rewrite (lemma size_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_all_list#1#nonneg
  (steps ((rewrite (lemma size_list_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_all_list#2#nonneg
  (steps ((rewrite (lemma size_list_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_all_if#0#decrease
  (have (= (le 0 (size el)) True)
    (rewrite-with (lemma size_nonneg) lr lhs () () refl)
    (have (= (le 0 (size t)) True)
      (rewrite-with (lemma size_nonneg) lr lhs () () refl)
      (steps ((simp lhs)) (by arith (list 1 1 1))))))
(proof-for
  rewrite_all_if#0#nonneg
  (steps ((rewrite (lemma size_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_all_if#1#decrease
  (have (= (le 0 (size el)) True)
    (rewrite-with (lemma size_nonneg) lr lhs () () refl)
    (have (= (le 0 (size c)) True)
      (rewrite-with (lemma size_nonneg) lr lhs () () refl)
      (steps ((simp lhs)) (by arith (list 1 1 1))))))
(proof-for
  rewrite_all_if#1#nonneg
  (steps ((rewrite (lemma size_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_all_if#2#decrease
  (have (= (le 0 (size t)) True)
    (rewrite-with (lemma size_nonneg) lr lhs () () refl)
    (have (= (le 0 (size c)) True)
      (rewrite-with (lemma size_nonneg) lr lhs () () refl)
      (steps ((simp lhs)) (by arith (list 1 1 1))))))
(proof-for
  rewrite_all_if#2#nonneg
  (steps ((rewrite (lemma size_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_all_if#3#decrease
  (have (= (le 0 (size t)) True)
    (rewrite-with (lemma size_nonneg) lr lhs () () refl)
    (have (= (le 0 (size c)) True)
      (rewrite-with (lemma size_nonneg) lr lhs () () refl)
      (steps ((simp lhs)) (by arith (list 1 0 1 1))))))
(proof-for
  rewrite_all_if#3#nonneg
  (steps ((rewrite (lemma size_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_all_if#4#decrease
  (have (= (le 0 (size el)) True)
    (rewrite-with (lemma size_nonneg) lr lhs () () refl)
    (have (= (le 0 (size c)) True)
      (rewrite-with (lemma size_nonneg) lr lhs () () refl)
      (steps ((simp lhs)) (by arith (list 1 0 1 1))))))
(proof-for
  rewrite_all_if#4#nonneg
  (steps ((rewrite (lemma size_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_all_if#5#decrease
  (have (= (le 0 (size t)) True)
    (rewrite-with (lemma size_nonneg) lr lhs () () refl)
    (have (= (le 0 (size c)) True)
      (rewrite-with (lemma size_nonneg) lr lhs () () refl)
      (steps ((simp lhs)) (by arith (list 1 0 1 1))))))
(proof-for
  rewrite_all_if#5#nonneg
  (steps ((rewrite (lemma size_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_all_if#6#decrease
  (have (= (le 0 (size t)) True)
    (rewrite-with (lemma size_nonneg) lr lhs () () refl)
    (have (= (le 0 (size c)) True)
      (rewrite-with (lemma size_nonneg) lr lhs () () refl)
      (steps ((simp lhs)) (by arith (list 1 0 0 1 1))))))
(proof-for
  rewrite_all_if#6#nonneg
  (steps ((rewrite (lemma size_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_nth#0#decrease
  (steps ((rewrite (premise 1) lr lhs true ()) (simp lhs)) (by arith (list))))
(proof-for
  rewrite_nth#0#nonneg
  (steps ((rewrite (lemma size_list_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_nth#1#decrease
  (steps ((rewrite (premise 1) lr lhs true ()) (simp lhs)) (by arith (list))))
(proof-for
  rewrite_nth#1#nonneg
  (steps ((rewrite (lemma size_list_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_nth#2#decrease
  (steps ((rewrite (premise 1) lr lhs true ()) (simp lhs)) (by arith (list))))
(proof-for
  rewrite_nth#2#nonneg
  (have (= (le 0 (size el)) True)
    (rewrite-with (lemma size_nonneg) lr lhs () () refl)
    (have (= (le 0 (size t)) True)
      (rewrite-with (lemma size_nonneg) lr lhs () () refl)
      (have (= (le 0 (size c)) True)
        (rewrite-with (lemma size_nonneg) lr lhs () () refl)
        (steps ((simp lhs)) (by arith (list 1 0 0 1 1 1)))))))
(proof-for
  rewrite_nth#3#decrease
  (have (= (le 0 (size_arms arms)) True)
    (rewrite-with (lemma size_arms_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 1) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 1)))))
(proof-for
  rewrite_nth#3#nonneg
  (steps ((rewrite (lemma size_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_nth#4#nonneg
  (steps ((rewrite (lemma size_arms_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_nth_list#0#nonneg
  (steps ((rewrite (lemma size_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_nth_list#1#nonneg
  (steps ((rewrite (lemma size_list_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_nth_if#0#decrease
  (have (= (le 0 (size el)) True)
    (rewrite-with (lemma size_nonneg) lr lhs () () refl)
    (have (= (le 0 (size t)) True)
      (rewrite-with (lemma size_nonneg) lr lhs () () refl)
      (steps ((simp lhs)) (by arith (list 1 1 1))))))
(proof-for
  rewrite_nth_if#0#nonneg
  (steps ((rewrite (lemma size_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_nth_if#1#decrease
  (have (= (le 0 (size el)) True)
    (rewrite-with (lemma size_nonneg) lr lhs () () refl)
    (have (= (le 0 (size c)) True)
      (rewrite-with (lemma size_nonneg) lr lhs () () refl)
      (steps ((simp lhs)) (by arith (list 1 1 1))))))
(proof-for
  rewrite_nth_if#1#nonneg
  (steps ((rewrite (lemma size_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_nth_if#2#decrease
  (have (= (le 0 (size t)) True)
    (rewrite-with (lemma size_nonneg) lr lhs () () refl)
    (have (= (le 0 (size c)) True)
      (rewrite-with (lemma size_nonneg) lr lhs () () refl)
      (steps ((simp lhs)) (by arith (list 1 1 1))))))
(proof-for
  rewrite_nth_if#2#nonneg
  (steps ((rewrite (lemma size_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_nth_arms#0#decrease
  (have (= (le 0 (size_arms t)) True)
    (rewrite-with (lemma size_arms_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 1)))))
(proof-for
  rewrite_nth_arms#0#nonneg
  (steps ((rewrite (lemma size_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_nth_arms#1#decrease
  (have (= (le 0 (size b)) True)
    (rewrite-with (lemma size_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 1)))))
(proof-for
  rewrite_nth_arms#1#nonneg
  (steps ((rewrite (lemma size_arms_nonneg) lr lhs true ())) refl))
(proof-for
  rewrite_all#4#decrease
  (have (= (le 0 (size scrut)) True)
    (rewrite-with (lemma size_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 1) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 0 1)))))
(proof-for
  rewrite_all_list#0#decrease
  (have (= (le 0 (size_list t)) True)
    (rewrite-with (lemma size_list_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 1)))))
(proof-for
  rewrite_all_list#1#decrease
  (have (= (le 0 (size h)) True)
    (rewrite-with (lemma size_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 0 1)))))
(proof-for
  rewrite_all_list#2#decrease
  (have (= (le 0 (size h)) True)
    (rewrite-with (lemma size_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 0 1)))))
(proof-for
  rewrite_nth#4#decrease
  (have (= (le 0 (size scrut)) True)
    (rewrite-with (lemma size_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 1) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 0 0 1)))))
(proof-for
  rewrite_nth_list#0#decrease
  (have (= (le 0 (size_list t)) True)
    (rewrite-with (lemma size_list_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 1)))))
(proof-for
  rewrite_nth_list#1#decrease
  (have (= (le 0 (size h)) True)
    (rewrite-with (lemma size_nonneg) lr lhs () () refl)
    (steps
      ((rewrite (premise 0) lr lhs true ()) (simp lhs))
      (by arith (list 1 0 0 0 1)))))
