;;; proof sidecar — MACHINE-OWNED, written by tools/prove (comments are
;;; not preserved). check REPLAYS these entries; it never searches.

(proof-for is_grammar_go#0#decrease (by arith (list)))
(proof-for is_grammar_go#0#nonneg (by arith (list 1 1)))
(proof-for is_grammar_go#1#decrease (by arith (list)))
(proof-for is_grammar_go#1#nonneg (by arith (list 1 1)))
(proof-for is_sweep#0#decrease (by arith (list)))
(proof-for is_sweep#0#nonneg (by arith (list 1 1)))
