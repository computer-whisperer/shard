;;; proof sidecar — MACHINE-OWNED, written by tools/prove (comments are
;;; not preserved). check REPLAYS these entries; it never searches.

(proof-for pc_raw_sweep#0#decrease (by arith (list)))
(proof-for pc_raw_sweep#0#nonneg (by arith (list 1 0 1)))
(proof-for pc_formed_sweep#0#decrease (by arith (list)))
(proof-for pc_formed_sweep#0#nonneg (by arith (list 1 0 1)))
