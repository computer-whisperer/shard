;;; proof sidecar — MACHINE-OWNED, written by tools/prove (comments are
;;; not preserved). check REPLAYS these entries; it never searches.

(proof-for cat_g#0#decrease (by arith (list 1 0 0 1)))
(proof-for cat_g#0#nonneg (steps ((compute both)) refl))
(proof-for cat_g#1#decrease (by arith (list 1 1 0 0 0)))
(proof-for cat_g#1#nonneg (by arith (list 1 0 0 0 1)))
(proof-for cat_g#2#decrease (by arith (list 1 0 0 1 0)))
(proof-for cat_g#2#nonneg (steps ((compute both)) refl))
(proof-for cat_g#3#decrease (by arith (list 1 1 0 0 0)))
(proof-for cat_g#3#nonneg (by arith (list 1 0 0 0 1)))
(proof-for cat_g#4#decrease (by arith (list 1 0 0 1 0)))
(proof-for cat_g#4#nonneg (steps ((compute both)) refl))
(proof-for cat_g#5#decrease (by arith (list)))
(proof-for cat_g#5#nonneg (by arith (list 1 0 0 1 0)))
(proof-for cat_g#6#decrease (by arith (list 1 0 0 1 0)))
(proof-for cat_g#6#nonneg (steps ((compute both)) refl))
(proof-for cat_g#7#decrease (by arith (list)))
(proof-for cat_g#7#nonneg (by arith (list 1 0 0 1 0)))
(proof-for cat_g#8#decrease (by arith (list 1 0 0 1 0)))
(proof-for cat_g#8#nonneg (steps ((compute both)) refl))
(proof-for cat_sweep#0#decrease (by arith (list)))
(proof-for cat_sweep#0#nonneg (by arith (list 1 1)))
(proof-for cat_sweep#1#decrease (by arith (list)))
(proof-for cat_sweep#1#nonneg (by arith (list 1 1)))
