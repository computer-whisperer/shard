;; expect: read-error type_mismatch
;; universe collapse: def bad : Prop := Nat — since slice 3.15 the elaborator refuses it before K
;; (Sort 1 against the expected Sort 0); K's own refusal of the raw declaration is hostile battery 1
(inductive Nat () Type (zero Nat) (succ (-> Nat Nat)))
(inductive Eq.{u} ((α (Sort u) implicit) (a α)) (-> α Prop) (refl (Eq a a)))
(def two () Nat (Nat.succ (Nat.succ Nat.zero)))
(def bad () Prop Nat)
