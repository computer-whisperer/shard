;; expect: List.append refused name_taken
;; a fn may not take an admitted constant's identity — module List's `append` is `List.append` — only a realize attaches an E body under one (§7.5)
(import Init Bool.casesOn)
(import "List.shard")
