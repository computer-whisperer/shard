;; expect: lib.peek.peek refused private_match
;; roots: lib/peek.shard
;; the private-equality leak (§6.5): a value of the view's sig type matched against a constructor —
;; here the implementation's, reached by a file inside the sealed directory importing it directly
;; (§6.3 item 5; from outside, the seal refuses the import first — private_module)
