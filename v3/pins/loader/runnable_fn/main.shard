;; expect: ok
;; an E form: read by the classifier (slice 5), recorded RUNNABLE — no L meaning, nothing in L cites it (§0)
(inductive Nat () Type (zero Nat) (succ (-> Nat Nat)))
(fn f ((x Nat)) Nat x)
(def z () Nat Nat.zero)
