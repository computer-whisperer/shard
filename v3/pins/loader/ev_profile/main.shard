;; expect: ok
;; roots: kernel/k.shard
;; the toolchain profile (§8): files under kernel/ are E only, flat scope, auto-bound type variables, Int numerals with -7, strings as bytes, symbols, (list …), the bare primitives
