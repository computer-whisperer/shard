;; expect: ok
;; roots: kernel/k.shard
;; a file that sees no Init (§8.1 rule 1; slice 3.4, formerly the toolchain profile): E only, its `use`
;; lines like any file, explicit type binders, numerals with -7, strings as bytes, symbols, (list …), the
;; operator primitives
