;; expect: ok
;; roots: main.shard lib
;; an implementation whose type is E only — a built-in field with no L constant in scope (§8.1
;; rule 1) — stands for a sig type as an E parameter: K admits the sig type as an opaque constant
;; for L citations, the consumer's E code sees no constructor, the fork matches the E-only type
;; by its arity (the K seal's own types are such)
(import "lib")
(use lib)
(fn f () T (empty))
