;; expect: main.g refused unknown_head
;; a head that resolves to nothing — Init's List.nil is an L constructor, not E-eligible at phase 2 (§11)
(import Init Nat)
(import Init List)
(fn g ((a Nat)) (List Nat) (List.cons a List.nil))
