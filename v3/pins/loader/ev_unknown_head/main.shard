;; expect: main.g refused unknown_head
;; a head that resolves to nothing (Init's List.cons is an E constructor since slice 5b — the earlier version of this case)
(import Init Nat)
(fn g ((a Nat)) Nat (nope a))
