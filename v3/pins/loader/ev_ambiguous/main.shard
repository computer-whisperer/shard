;; expect: main.h refused ambiguous_name
;; two opened modules declare g: the citation is ambiguous, as an L citation would be (§3.1)
(import Init Nat)
(import "a.shard")
(import "b.shard")
(use a)
(use b)
(fn h () Nat (g))
