;; expect: load-error private_module
;; the sealed-directory rule (§13 item 26): lib/ has a view, so its files are private to it —
;; a direct import of lib/helper.shard from outside is refused; `(import "lib")` is the way in
(import "lib/helper.shard")
(fn f () Int (h))
