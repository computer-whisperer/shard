;; expect: main.loop refused realize_recursion
;; §8.4 rule 1: an eligible fn whose definition fails is refused, never RUNNABLE — here a
;; self-call without a measure
(import Init Int.decEq)
(fn loop ((n Nat)) Nat (loop n))
