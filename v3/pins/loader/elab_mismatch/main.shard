;; expect: read-error type_mismatch
;; slice 3.15: the one coercion is Nat → Int; an Int where a Nat is expected is a type error
;; naming both types (law §5.2: Int.toNat is a named conversion, never inserted)
(import Init Int.decEq)
(def bad () Nat (Nat.succ (Int.ofNat 1)))
