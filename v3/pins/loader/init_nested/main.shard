;; expect: ok
;; two nested prefixes: a loaded through Nat, this file through Bool — the larger once, on from where a stopped
(import "a.shard")
(import Init Bool)
(def t () Bool Bool.true)
(def z2 () Nat a.z)
