;; expect: ok
;; a symbol literal in every file (§8.1 rule 1; slice 3.4): Symbol is a built-in E type, the interned atom;
;; before, S refused it (symbol_literal)
(import Init Bool.casesOn)
(fn g () Symbol (quote x))
(fn same ((s Symbol)) Bool (sym_eq s (quote x)))
