;; expect: main.twice refused no_l_identity
;; the profile's `+` has no L identity: a realization writes the naming-law spelling
(import Init Bool.casesOn)
(def twice ((n Nat)) Nat (Nat.add n n))
(realize twice ((n Nat)) Nat (+ n n))
