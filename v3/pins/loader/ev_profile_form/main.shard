;; expect: read-error unknown_constant
;; roots: kernel/k.shard
;; a def in a file without Init citing Int: Int is a built-in E type with no L constant in scope (§8.1 rule 1),
;; so K's reader refuses the citation — no form is refused by its directory (formerly profile_form)
