;; the implementation: the concrete Stack, the fns (bodies read at slice 5), the law proved
(import Init Eq)
(type Stack (mk (List Nat)))
(fn empty () Stack (Stack.mk List.nil))
(fn push ((n Nat) (s Stack)) Stack s)
(fulfills stack_refl (exact (Eq.refl.{1} Stack s)))
