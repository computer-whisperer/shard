;; expect: ok
;; roots: main.shard lib
;; a consumer against the view alone: its theorem rests on the view's parameters (params=); then lib itself, its implementation checked
(import Init Eq)
(import "lib")
(use lib)
(theorem t ((s Stack)) (Eq.{1} Stack (push Nat.zero s) (push Nat.zero s)) (exact (push_refl Nat.zero s)))
