;; expect: main.List.append.realize_2 refused conversion
;; T1: a fresh but semantically wrong executable body — its second equation is not a theorem, K refuses it, nothing is attached
(import Init Bool.casesOn)
(realize List.append ((a Type) (xs (List a)) (ys (List a))) (List a) (measure (struct xs))
  (match xs (List.nil ys) ((List.cons x t) (List.append t ys))))
