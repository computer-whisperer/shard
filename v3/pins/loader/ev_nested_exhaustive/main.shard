;; expect: ok
;; nested patterns: the matrix specializes each constructor, a variable row closes the rest
(import Init Nat)
(type (Lst T) (nil) (cons T (Lst T)))
(fn second ((xs (Lst Nat))) Nat
  (match xs
    ((cons _ (cons y _)) y)
    ((cons x nil) x)
    (nil 0)))
