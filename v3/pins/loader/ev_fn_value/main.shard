;; expect: main.g refused type_mismatch
;; a function name in argument position (law §4.3; §6.3 item 2): on the typed route (slice 3.16
;; rule 6) it is a function where a Nat is expected — the elaborator's refusal
(import Init Nat)
(fn f ((a Nat)) Nat a)
(fn h ((a Nat)) Nat a)
(fn g ((a Nat)) Nat (h f))
