;; expect: main.g refused function_value
;; a function name in argument position is the escape rule's refusal (law §4.3; §6.3 item 2)
(import Init Nat)
(fn f ((a Nat)) Nat a)
(fn h ((a Nat)) Nat a)
(fn g ((a Nat)) Nat (h f))
