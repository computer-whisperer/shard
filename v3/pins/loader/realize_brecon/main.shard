;; expect: List.flatten refused recursion_structure
;; §7.3: a recursive definition's kernel term is brecOn applied to a functional — no executable view is readable off it
(import Init Bool.casesOn)
(realize List.flatten (view))
