;; expect: ok
;; the pinned export through Eq (the 3000-line fixture); Init names are bare; (use Init.Nat) opens Nat
(import Init Eq)
(use Init.Nat)
(def n () Nat (succ Nat.zero))
(theorem n_rfl () (Eq n n) (exact (Eq.refl n)))
