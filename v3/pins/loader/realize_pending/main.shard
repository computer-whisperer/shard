;; expect: main.id2 pending measure
;; a measure other than struct is a reported obligation until phase 3's tactics (§7.2); the realization is attached
(import Init Bool.casesOn)
(def id2 ((xs (List Nat))) (List Nat) xs)
(realize id2 ((xs (List Nat))) (List Nat) (measure (Nat.add 1 1)) xs)
