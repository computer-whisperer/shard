;; expect: load-error duplicate_name
;; two modules construct one identity (§3; GPT-6 R61): `a.shard` declaring `b.c` and `a/b.shard` declaring
;; `c` are both `a.b.c` — the second is refused, deterministically, never conflated
(import Init Nat)
(import "a.shard")
(import "a/b.shard")
(fn go () Nat (a.b.c))
