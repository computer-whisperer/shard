;; expect: ok
;; §7.1: the derived view — a structure projection, a wrapper over a supplied realization, a decision on an erased proof
(import Init Bool.casesOn)
(realize List.concat ((a Type) (xs (List a)) (y a)) (List a) (measure (struct xs))
  (match xs (List.nil (List.cons y List.nil)) ((List.cons x t) (List.cons x (List.concat t y)))))
(realize Array.toList (view))
(realize Array.push (view))
(realize dite (view))
(realize ite (view))
(fn push2 ((a (Array Nat))) (Array Nat) (Array.push (Array.push a 1) 2))
