;; expect: read-error bad_constructor
;; roots: main.shard b.shard
;; a file whose reading fails after its heads were pre-registered is loaded once (slice 6): the
;; second root's import of it reports import_failed, never duplicate_name from a re-read
(import "broken.shard")
