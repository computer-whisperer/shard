;; expect: ok
;; roots: main.shard lib
;; the sealed-directory rule's other side: lib's implementation imports the private helper from
;; inside lib/, and the consumer imports the directory
(import Init Nat)
(import "lib")
(use lib)
(fn f () T (empty))
