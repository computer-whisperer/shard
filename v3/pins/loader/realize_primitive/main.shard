;; expect: Nat.mul refused realize_primitive
;; an entry of the primitive table is realized by K's own rule (§6.4, §7.5): a body cites it as a primitive
(import Init Bool.casesOn)
(realize Nat.mul ((n Nat) (m Nat)) Nat (Nat.add n m))
