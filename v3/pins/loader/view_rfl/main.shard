;; expect: main.t refused conversion
;; roots: main.shard lib
;; §8.5: K cannot unfold a view's parameter — a consumer's Eq.refl through the implementation's
;; body is refused; the requirement step_pos is what a consumer cites (view_basic)
(import Init Int.decEq)
(import "lib")
(use lib)
(theorem t ((n Nat)) (Eq.{1} Nat (step n) (Nat.add n 1)) (exact (Eq.refl.{1} Nat (Nat.add n 1))))
