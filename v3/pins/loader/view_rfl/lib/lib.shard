;; the implementation: the fn is defined in the fork, its equation stated there, the law proved by it
(import Init Int.decEq)
(fn step ((n Nat)) Nat (+ n 1))
(fulfills step_pos (exact (step.eq_1 n)))
