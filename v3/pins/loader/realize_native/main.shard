;; expect: ok
;; a native def realized: one equation, a tautology through the table's Nat.add, proven by K's literal rule
(import Init Bool.casesOn)
(def twice ((n Nat)) Nat (Nat.add n n))
(realize twice ((n Nat)) Nat (Nat.add n n))
(fn four () Nat (twice 2))
