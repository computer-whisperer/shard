;; expect: lib.peek.peek refused private_match
;; roots: lib/peek.shard
;; the leak through a polymorphic wrapper: the static type is inferred from the constructor's
;; arguments (§6.7); the consumer sits inside the sealed directory (ev_private_match)
