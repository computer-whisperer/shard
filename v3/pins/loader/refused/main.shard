;; expect: read-error type_mismatch
;; a false theorem: since slice 3.15 the elaborator refuses the proof's type before K (K's own
;; refusal of the raw declaration is hostile battery 1; the reader pin refuse_false_theorem)
(inductive Nat () Type (zero Nat) (succ (-> Nat Nat)))
(inductive Eq.{u} ((α (Sort u) implicit) (x α)) (-> α Prop) (refl (Eq x x)))
(def two () Nat (Nat.succ (Nat.succ Nat.zero)))
(theorem bad () (Eq two Nat.zero) (exact (Eq.refl two)))
(def after () Nat two)
