;; expect: read-error instance_needed
;; slice 3.15: an instance-implicit binder has no resolution at Stage 1 (typeclasses are Stage 3);
;; the refusal points to @ with the instance written
(import Init Int.decEq)
(def bad () Nat (ite True 1 2))
