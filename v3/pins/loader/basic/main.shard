;; expect: ok
;; import brings a's identities into scope; use opens the prefix; the qualified name always resolves
(import "a.shard")
(use a)
(def three () Nat (Nat.succ two))
(def three' () a.Nat (a.Nat.succ a.two))
(theorem three_rfl () (Eq three three') (exact (Eq.refl three)))
