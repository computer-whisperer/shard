;; module a: the world's Nat and Eq, a definition and a proof
(inductive Nat () Type (zero Nat) (succ (-> Nat Nat)))
(inductive Eq.{u} ((α (Sort u) implicit) (x α)) (-> α Prop) (refl (Eq.{u} α x x)))
(def two () Nat (Nat.succ (Nat.succ Nat.zero)))
(theorem two_rfl () (Eq.{1} Nat two two) (exact (Eq.refl.{1} Nat two)))
