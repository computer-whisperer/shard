;; expect: ok
;; (list …) in every file (§8.1 rule 2; slice 3.4): the constructor chain of the List in scope — Init's
;; here; before, S refused it (list_sugar) pending Stage 1's list literal
(import Init Bool.casesOn)
(fn g () (List Nat) (list 1 2))
(fn h ((xs (List Nat))) Nat (match xs (List.nil 0) ((List.cons x _) x)))
