;; expect: ok
;; roots: main.shard lib
;; §8.4 slice 3.16 rule 1 (GPT-6 R63, R64; fixture G9): a matcher through a view. `depth` is
;; defined inside the implementation check with a `match`: its matcher is generated, admitted
;; and read back in the fork — the description is the matcher's type, so nothing is lost at the
;; fork point — its program is the erasure, and the requirement is proved by computing through
;; it (Eq.refl). The consumer sees the view alone
(import Init Int.decEq)
(import "lib")
(use lib)
(theorem t () (Eq (depth (empty)) 0) (exact depth_empty))
