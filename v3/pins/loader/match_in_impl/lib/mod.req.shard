;; the view: an opaque Stack, its operations as signatures, a law about them
(import Init Int.decEq)
(sig type Stack)
(sig fn empty () Stack)
(sig fn push ((n Nat) (s Stack)) Stack)
(sig fn depth ((s Stack)) Nat)
(requirement depth_empty () (Eq (depth (empty)) 0))
