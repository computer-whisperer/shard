;; expect: main.down runnable measure_type
;; §8.4 (slice 3.14 rule 2): an Int-valued measure is an obstacle — the law inserts no Int.toNat;
;; write a Nat measure
(import Init InvImage.wf)
(fn down ((n Int)) Int (measure n) (if (lt 0 n) (down (- n 1)) 0))
