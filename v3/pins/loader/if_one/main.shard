;; expect: main.one refused if_type
;; an `if` on a one-constructor type, and on a Nat (§6.2; GPT-6 R56): neither is a decision
(import Init Nat)
(type One (Only))
(fn one ((x One)) Nat (if x 1 0))
(fn n ((k Nat)) Nat (if k 1 0))
