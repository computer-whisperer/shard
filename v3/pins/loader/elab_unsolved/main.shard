;; expect: read-error unsolved_implicit
;; slice 3.15: an implicit the explicit arguments and the expected type do not determine is a
;; refusal naming the binder, with the pointer to @ (law §5.2: reconstruction, never a default)
(import Init Int.decEq)
(def bad () Prop (= List.nil List.nil))
