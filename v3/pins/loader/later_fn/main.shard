;; expect: main.f later fn
;; an E form: its reader lands with the classifier (slice 5); the loader reports and continues
(inductive Nat () Type (zero Nat) (succ (-> Nat Nat)))
(fn f ((x Nat)) Nat x)
(def z () Nat Nat.zero)
