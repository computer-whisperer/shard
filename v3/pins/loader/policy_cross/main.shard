;; expect: main.t2 policy a.ax
;; the policy is per file: a trusts its axiom, this file does not, so a theorem resting on it is outside the policy here
(import "a.shard")
(use a)
(theorem t2 () (Eq.{1} Nat two two) (exact t))
