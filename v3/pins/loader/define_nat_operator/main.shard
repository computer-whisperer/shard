;; expect: main.pred refused nat_operator
;; §8.4 rule 3: `-` at Nat has no identity ev and K agree on (Nat.sub truncates where ev computes
;; the integer difference) — write Nat.sub; the fn is eligible, so this is a refusal
(import Init Int.decEq)
(fn pred ((n Nat)) Nat (- n 1))
