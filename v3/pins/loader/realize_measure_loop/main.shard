;; expect: main.twice refused realize_descent
;; T1: a self-call on the parameters themselves never descends, whatever the measure — refused before K states `twice n = twice n`
(import Init Bool.casesOn)
(def twice ((n Nat)) Nat (Nat.add n n))
(realize twice ((n Nat)) Nat (measure n) (twice n))
