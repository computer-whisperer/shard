;; expect: List.map refused realize_signature
;; a function-typed binder has no E signature (law §4.3): List.map waits for templates (phase 3)
(import Init Bool.casesOn)
(realize List.map ((a Type) (b Type) (xs (List a))) (List b) List.nil)
