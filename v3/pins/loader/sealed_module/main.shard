;; expect: ok
;; roots: main.shard lib
;; one module across a sealed directory (§13 item 26, the K seal): the view's sig type and sig fn
;; are implemented in two private files, whose declarations take the directory's module path —
;; lib.T in types.shard, lib.empty in ops.shard — and the implementation file only imports them
(import Init Nat)
(import "lib")
(use lib)
(fn f () T (empty))
