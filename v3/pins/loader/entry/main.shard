;; expect: ok
;; roots: kernel/main.shard
;; the checked entry (§9, slice 7): `main` takes an Int, a byte list and the World last, and the
;; driver validates `-- ARG…` against the two parameters before ev runs (kernel/test/entry_test.shard
;; runs it); `raw` takes the World alone and reads the argument list itself through get_args —
;; the entry declaring its own precondition; `opt` and `noworld` are entries the driver refuses
