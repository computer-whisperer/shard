;;; v3/examples/calc/calc_spec_tests.shard — examples/calc/calc_spec_tests.shard
;;; at slice 6: nine ground claims over spec_run and no program; the claims
;;; wait for phase 3's I (they become `decide`/`rfl` theorems). Kept for the
;;; graph's sake, file for file.

(import "calc_spec.shard")
