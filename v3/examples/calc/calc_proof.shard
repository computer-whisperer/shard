;;; v3/examples/calc/calc_proof.shard — examples/calc/calc_proof.shard at
;;; slice 6: the file held six claims about the lexer (is_digit_of_digit and
;;; its kin) and no program; the claims wait for phase 3's I. It stays as the
;;; module the old tree's import graph names (calc_ndigit and calc_show_run
;;; import it), so the port's graph is the old one file for file.

(import "calc.shard")
