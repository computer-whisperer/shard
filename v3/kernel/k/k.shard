;;; v3/kernel/k/k.shard — K's implementation: the private files of the sealed directory,
;;; one module with the view (mod.req.shard). Nothing is declared here; the view's
;;; signatures are matched against the declarations these imports bring.

(import "env.shard")
(import "tc.shard")
(import "add.shard")
(import "inductive.shard")
(import "nested.shard")
(import "import.shard")
(import "axioms.shard")
(import "accel_pins.shard")
(import "refgen.shard")
