;;; v3/kernel/accel_pins.shard — GENERATED by v3/kernel/gen_pins.sh from the
;;; pinned Init export (v3/README.md pin); never hand-edited. The fixed
;;; identities of FOUNDATION §3.2: the structural hash (expr.shard
;;; hash_expr) of each accelerated or literal-bearing declaration's
;;; admitted type and value. K enables an accelerator, a literal typing
;;; rule or a literal expansion only for a declaration whose admitted
;;; identity matches its row here.
;;;
;;; STATUS: generated 2026-09-06 from probe_0000 probe_0001 

(import "expr.shard")

;; the declarations K accelerates or types literals with
(fn pin_candidates () (List Name)
  (list (n1 (quote Nat)) (n1 (quote String)) (n1 (quote Char)) (n1 (quote List))
        (n2 (quote String) (quote ofList)) (n2 (quote Char) (quote ofNat))
        (n2 (quote Nat) (quote add)) (n2 (quote Nat) (quote sub)) (n2 (quote Nat) (quote mul)) (n2 (quote Nat) (quote pow))
        (n2 (quote Nat) (quote gcd)) (n2 (quote Nat) (quote mod)) (n2 (quote Nat) (quote div)) (n2 (quote Nat) (quote beq))
        (n2 (quote Nat) (quote ble)) (n2 (quote Nat) (quote land)) (n2 (quote Nat) (quote lor)) (n2 (quote Nat) (quote xor))
        (n2 (quote Nat) (quote shiftLeft)) (n2 (quote Nat) (quote shiftRight))))

(fn is_pin_candidate ((n Name)) Bool (mem_name n (pin_candidates)))

(fn accel_pin ((n Name)) (Option Int)
  (if (name_eq n (Str Anon (quote Nat))) (Some 442623504116621704)
  (if (name_eq n (Str Anon (quote String))) (Some 1378298069812926080)
  (if (name_eq n (Str Anon (quote Char))) (Some 1381718674064941822)
  (if (name_eq n (Str Anon (quote List))) (Some 2091216748897528233)
  (if (name_eq n (Str (Str Anon (quote Nat)) (quote add))) (Some 606620205132656612)
  (if (name_eq n (Str (Str Anon (quote Nat)) (quote sub))) (Some 1073104838883077964)
  (if (name_eq n (Str (Str Anon (quote Nat)) (quote mul))) (Some 446734101742310599)
  (if (name_eq n (Str (Str Anon (quote Nat)) (quote pow))) (Some 1387980402654970397)
  (if (name_eq n (Str (Str Anon (quote Nat)) (quote gcd))) (Some 2182957055885862721)
  (if (name_eq n (Str (Str Anon (quote Nat)) (quote mod))) (Some 1447203708693152183)
  (if (name_eq n (Str (Str Anon (quote Nat)) (quote div))) (Some 2200034700744244249)
  (if (name_eq n (Str (Str Anon (quote Nat)) (quote beq))) (Some 1284857677603515920)
  (if (name_eq n (Str (Str Anon (quote Nat)) (quote ble))) (Some 1995285773580152216)
  (if (name_eq n (Str (Str Anon (quote Nat)) (quote land))) (Some 1890026353597634757)
  (if (name_eq n (Str (Str Anon (quote Nat)) (quote shiftRight))) (Some 2211624485527950144)
  None))))))))))))))))
