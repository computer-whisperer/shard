;;; proof sidecar — MACHINE-OWNED, written by tools/prove (comments are
;;; not preserved). check REPLAYS these entries; it never searches.

(proof-for str_len_is_blen (steps ((compute both)) refl))
(proof-for
  str_valid
  (refine-fact
    Str
    s
    (steps ((unfold bytes_of lhs) (rewrite (premise 0) lr lhs true ())) refl)))
(proof-for
  str_len_nonneg
  (steps
    ((unfold str_len lhs) (rewrite (lemma blen_nonneg) lr lhs true ()))
    refl))
