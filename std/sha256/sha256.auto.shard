;;; proof sidecar — MACHINE-OWNED, written by tools/prove (comments are
;;; not preserved). check REPLAYS these entries; it never searches.

(proof-for szeros#0#decrease (by arith (list)))
(proof-for szeros#0#nonneg (by arith (list 1 1)))
(proof-for words16#0#decrease (by arith (list)))
(proof-for words16#0#nonneg (by arith (list 1 1)))
(proof-for sched_ext#0#decrease (by arith (list)))
(proof-for sched_ext#0#nonneg (by arith (list 1 1)))
(proof-for sha_blocks#0#decrease (by arith (list)))
(proof-for sha_blocks#0#nonneg (by arith (list 1 1)))
(proof-for
  sha256_len
  (steps ((rewrite (lemma len_sha256) lr lhs true ())) refl))
