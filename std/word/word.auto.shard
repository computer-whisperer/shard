;;; proof sidecar — MACHINE-OWNED, written by tools/prove (comments are
;;; not preserved). check REPLAYS these entries; it never searches.

(proof-for u8_val_of (steps ((compute both)) refl))
(proof-for
  u8_made_lo
  (steps
    ((unfold u8 lhs) (simp lhs))
    (have (= (lt (mod nn 256) 256) True)
      (rewrite-with
        (lemma mod_hi)
        lr
        lhs
        ()
        ((steps ((compute both)) refl))
        refl)
      (have (= (le 0 (mod nn 256)) True)
        (rewrite-with
          (lemma mod_lo)
          lr
          lhs
          ()
          ((steps ((compute both)) refl))
          refl)
        (by arith (list 1 0 1))))))
(proof-for
  u8_made_hi
  (steps
    ((unfold u8 lhs) (simp lhs))
    (have (= (lt (mod nn 256) 256) True)
      (rewrite-with
        (lemma mod_hi)
        lr
        lhs
        ()
        ((steps ((compute both)) refl))
        refl)
      (by arith (list 1 1)))))
(proof-for u8_add_val (steps ((compute both)) refl))
(proof-for u8_sub_val (steps ((compute both)) refl))
(proof-for u8_mul_val (steps ((compute both)) refl))
(proof-for u8_and_val (steps ((compute both)) refl))
(proof-for u8_or_val (steps ((compute both)) refl))
(proof-for u8_xor_val (steps ((compute both)) refl))
(proof-for u8_not_val (steps ((compute both)) refl))
(proof-for u8_eq_val (steps ((compute both)) refl))
(proof-for u8_lt_val (steps ((compute both)) refl))
(proof-for u8_le_val (steps ((compute both)) refl))
(proof-for u8_shl_val (steps ((compute both)) refl))
(proof-for u8_shr_val (steps ((compute both)) refl))
(proof-for i8_val_of (steps ((compute both)) refl))
(proof-for
  i8_wrap_lo
  (case-on (lt (mod nn 256) 128) Bool
    ((case False
       (steps
         ((unfold i8_wrap lhs) (rewrite (hyp 0) lr lhs true ()) (simp lhs))
         (have (= (lt (mod nn 256) 128) False)
           (steps ((rewrite (hyp 0) lr lhs true ())) refl)
           (by arith (list 1 1)))))
     (case True
       (steps
         ((unfold i8_wrap lhs) (rewrite (hyp 0) lr lhs true ()) (simp lhs))
         (have (= (lt (mod nn 256) 128) True)
           (steps ((rewrite (hyp 0) lr lhs true ())) refl)
           (have (= (lt (mod nn 256) 256) True)
             (rewrite-with
               (lemma mod_hi)
               lr
               lhs
               ()
               ((steps ((compute both)) refl))
               refl)
             (have (= (le 0 (mod nn 256)) True)
               (rewrite-with
                 (lemma mod_lo)
                 lr
                 lhs
                 ()
                 ((steps ((compute both)) refl))
                 refl)
               (by arith (list 1 0 0 1))))))))))
(proof-for
  i8_wrap_hi
  (case-on (lt (mod nn 256) 128) Bool
    ((case False
       (steps
         ((unfold i8_wrap lhs) (rewrite (hyp 0) lr lhs true ()) (simp lhs))
         (have (= (lt (mod nn 256) 128) False)
           (steps ((rewrite (hyp 0) lr lhs true ())) refl)
           (have (= (lt (mod nn 256) 256) True)
             (rewrite-with
               (lemma mod_hi)
               lr
               lhs
               ()
               ((steps ((compute both)) refl))
               refl)
             (by arith (list 1 0 1))))))
     (case True
       (steps
         ((unfold i8_wrap lhs) (rewrite (hyp 0) lr lhs true ()) (simp lhs))
         (have (= (lt (mod nn 256) 128) True)
           (steps ((rewrite (hyp 0) lr lhs true ())) refl)
           (by arith (list 1 1))))))))
(proof-for i8_add_val (steps ((compute both)) refl))
(proof-for i8_sub_val (steps ((compute both)) refl))
(proof-for i8_mul_val (steps ((compute both)) refl))
(proof-for i8_eq_val (steps ((compute both)) refl))
(proof-for i8_slt_val (steps ((compute both)) refl))
(proof-for i8_sle_val (steps ((compute both)) refl))
(proof-for i8_shl_val (steps ((compute both)) refl))
(proof-for i8_sshr_val (steps ((compute both)) refl))
(proof-for u16_val_of (steps ((compute both)) refl))
(proof-for
  u16_made_lo
  (steps
    ((unfold u16 lhs) (simp lhs))
    (have (= (lt (mod nn 65536) 65536) True)
      (rewrite-with
        (lemma mod_hi)
        lr
        lhs
        ()
        ((steps ((compute both)) refl))
        refl)
      (have (= (le 0 (mod nn 65536)) True)
        (rewrite-with
          (lemma mod_lo)
          lr
          lhs
          ()
          ((steps ((compute both)) refl))
          refl)
        (by arith (list 1 0 1))))))
(proof-for
  u16_made_hi
  (steps
    ((unfold u16 lhs) (simp lhs))
    (have (= (lt (mod nn 65536) 65536) True)
      (rewrite-with
        (lemma mod_hi)
        lr
        lhs
        ()
        ((steps ((compute both)) refl))
        refl)
      (by arith (list 1 1)))))
(proof-for u16_add_val (steps ((compute both)) refl))
(proof-for u16_sub_val (steps ((compute both)) refl))
(proof-for u16_mul_val (steps ((compute both)) refl))
(proof-for u16_and_val (steps ((compute both)) refl))
(proof-for u16_or_val (steps ((compute both)) refl))
(proof-for u16_xor_val (steps ((compute both)) refl))
(proof-for u16_not_val (steps ((compute both)) refl))
(proof-for u16_eq_val (steps ((compute both)) refl))
(proof-for u16_lt_val (steps ((compute both)) refl))
(proof-for u16_le_val (steps ((compute both)) refl))
(proof-for u16_shl_val (steps ((compute both)) refl))
(proof-for u16_shr_val (steps ((compute both)) refl))
(proof-for u32_val_of (steps ((compute both)) refl))
(proof-for
  u32_made_lo
  (steps
    ((unfold u32 lhs) (simp lhs))
    (have (= (lt (mod nn 4294967296) 4294967296) True)
      (rewrite-with
        (lemma mod_hi)
        lr
        lhs
        ()
        ((steps ((compute both)) refl))
        refl)
      (have (= (le 0 (mod nn 4294967296)) True)
        (rewrite-with
          (lemma mod_lo)
          lr
          lhs
          ()
          ((steps ((compute both)) refl))
          refl)
        (by arith (list 1 0 1))))))
(proof-for
  u32_made_hi
  (steps
    ((unfold u32 lhs) (simp lhs))
    (have (= (lt (mod nn 4294967296) 4294967296) True)
      (rewrite-with
        (lemma mod_hi)
        lr
        lhs
        ()
        ((steps ((compute both)) refl))
        refl)
      (by arith (list 1 1)))))
(proof-for u32_add_val (steps ((compute both)) refl))
(proof-for u32_sub_val (steps ((compute both)) refl))
(proof-for u32_mul_val (steps ((compute both)) refl))
(proof-for u32_and_val (steps ((compute both)) refl))
(proof-for u32_or_val (steps ((compute both)) refl))
(proof-for u32_xor_val (steps ((compute both)) refl))
(proof-for u32_not_val (steps ((compute both)) refl))
(proof-for u32_eq_val (steps ((compute both)) refl))
(proof-for u32_lt_val (steps ((compute both)) refl))
(proof-for u32_le_val (steps ((compute both)) refl))
(proof-for u32_shl_val (steps ((compute both)) refl))
(proof-for u32_shr_val (steps ((compute both)) refl))
(proof-for i16_val_of (steps ((compute both)) refl))
(proof-for
  i16_wrap_lo
  (case-on (lt (mod nn 65536) 32768) Bool
    ((case False
       (steps
         ((unfold i16_wrap lhs) (rewrite (hyp 0) lr lhs true ()) (simp lhs))
         (have (= (lt (mod nn 65536) 32768) False)
           (steps ((rewrite (hyp 0) lr lhs true ())) refl)
           (by arith (list 1 1)))))
     (case True
       (steps
         ((unfold i16_wrap lhs) (rewrite (hyp 0) lr lhs true ()) (simp lhs))
         (have (= (lt (mod nn 65536) 32768) True)
           (steps ((rewrite (hyp 0) lr lhs true ())) refl)
           (have (= (lt (mod nn 65536) 65536) True)
             (rewrite-with
               (lemma mod_hi)
               lr
               lhs
               ()
               ((steps ((compute both)) refl))
               refl)
             (have (= (le 0 (mod nn 65536)) True)
               (rewrite-with
                 (lemma mod_lo)
                 lr
                 lhs
                 ()
                 ((steps ((compute both)) refl))
                 refl)
               (by arith (list 1 0 0 1))))))))))
(proof-for
  i16_wrap_hi
  (case-on (lt (mod nn 65536) 32768) Bool
    ((case False
       (steps
         ((unfold i16_wrap lhs) (rewrite (hyp 0) lr lhs true ()) (simp lhs))
         (have (= (lt (mod nn 65536) 32768) False)
           (steps ((rewrite (hyp 0) lr lhs true ())) refl)
           (have (= (lt (mod nn 65536) 65536) True)
             (rewrite-with
               (lemma mod_hi)
               lr
               lhs
               ()
               ((steps ((compute both)) refl))
               refl)
             (by arith (list 1 0 1))))))
     (case True
       (steps
         ((unfold i16_wrap lhs) (rewrite (hyp 0) lr lhs true ()) (simp lhs))
         (have (= (lt (mod nn 65536) 32768) True)
           (steps ((rewrite (hyp 0) lr lhs true ())) refl)
           (by arith (list 1 1))))))))
(proof-for i16_add_val (steps ((compute both)) refl))
(proof-for i16_sub_val (steps ((compute both)) refl))
(proof-for i16_mul_val (steps ((compute both)) refl))
(proof-for i16_eq_val (steps ((compute both)) refl))
(proof-for i16_slt_val (steps ((compute both)) refl))
(proof-for i16_sle_val (steps ((compute both)) refl))
(proof-for i16_shl_val (steps ((compute both)) refl))
(proof-for i16_sshr_val (steps ((compute both)) refl))
(proof-for i32_val_of (steps ((compute both)) refl))
(proof-for
  i32_wrap_lo
  (case-on (lt (mod nn 4294967296) 2147483648) Bool
    ((case False
       (steps
         ((unfold i32_wrap lhs) (rewrite (hyp 0) lr lhs true ()) (simp lhs))
         (have (= (lt (mod nn 4294967296) 2147483648) False)
           (steps ((rewrite (hyp 0) lr lhs true ())) refl)
           (by arith (list 1 1)))))
     (case True
       (steps
         ((unfold i32_wrap lhs) (rewrite (hyp 0) lr lhs true ()) (simp lhs))
         (have (= (lt (mod nn 4294967296) 2147483648) True)
           (steps ((rewrite (hyp 0) lr lhs true ())) refl)
           (have (= (lt (mod nn 4294967296) 4294967296) True)
             (rewrite-with
               (lemma mod_hi)
               lr
               lhs
               ()
               ((steps ((compute both)) refl))
               refl)
             (have (= (le 0 (mod nn 4294967296)) True)
               (rewrite-with
                 (lemma mod_lo)
                 lr
                 lhs
                 ()
                 ((steps ((compute both)) refl))
                 refl)
               (by arith (list 1 0 0 1))))))))))
(proof-for
  i32_wrap_hi
  (case-on (lt (mod nn 4294967296) 2147483648) Bool
    ((case False
       (steps
         ((unfold i32_wrap lhs) (rewrite (hyp 0) lr lhs true ()) (simp lhs))
         (have (= (lt (mod nn 4294967296) 2147483648) False)
           (steps ((rewrite (hyp 0) lr lhs true ())) refl)
           (have (= (lt (mod nn 4294967296) 4294967296) True)
             (rewrite-with
               (lemma mod_hi)
               lr
               lhs
               ()
               ((steps ((compute both)) refl))
               refl)
             (by arith (list 1 0 1))))))
     (case True
       (steps
         ((unfold i32_wrap lhs) (rewrite (hyp 0) lr lhs true ()) (simp lhs))
         (have (= (lt (mod nn 4294967296) 2147483648) True)
           (steps ((rewrite (hyp 0) lr lhs true ())) refl)
           (by arith (list 1 1))))))))
(proof-for i32_add_val (steps ((compute both)) refl))
(proof-for i32_sub_val (steps ((compute both)) refl))
(proof-for i32_mul_val (steps ((compute both)) refl))
(proof-for i32_eq_val (steps ((compute both)) refl))
(proof-for i32_slt_val (steps ((compute both)) refl))
(proof-for i32_sle_val (steps ((compute both)) refl))
(proof-for i32_shl_val (steps ((compute both)) refl))
(proof-for i32_sshr_val (steps ((compute both)) refl))
