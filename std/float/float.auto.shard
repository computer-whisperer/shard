;;; proof sidecar — MACHINE-OWNED, written by tools/prove (comments are
;;; not preserved). check REPLAYS these entries; it never searches.

(proof-for fpow2#0#decrease (by arith (list)))
(proof-for fpow2#0#nonneg (by arith (list 1 1)))
(proof-for
  ibits#0#nonneg
  (have h1 (= n (+ (* 2 (ediv n 2)) (mod n 2)))
    (rewrite-with
      (lemma ediv_mod_id)
      lr
      lhs
      ((inst n n) (inst d 2))
      ((steps ((compute both)) refl))
      refl)
    (have h2 (= (le 0 (mod n 2)) True)
      (rewrite-with
        (lemma mod_lo)
        lr
        lhs
        ()
        ((steps ((compute both)) refl))
        refl)
      (have h3 (= (lt (mod n 2) 2) True)
        (rewrite-with
          (lemma mod_hi)
          lr
          lhs
          ()
          ((steps ((compute both)) refl))
          refl)
        (steps () (by arith (list 2 1 -1 0 1)))))))
(proof-for
  ibits#0#decrease
  (have h1 (= n (+ (* 2 (ediv n 2)) (mod n 2)))
    (rewrite-with
      (lemma ediv_mod_id)
      lr
      lhs
      ((inst n n) (inst d 2))
      ((steps ((compute both)) refl))
      refl)
    (have h2 (= (le 0 (mod n 2)) True)
      (rewrite-with
        (lemma mod_lo)
        lr
        lhs
        ()
        ((steps ((compute both)) refl))
        refl)
      (have h3 (= (lt (mod n 2) 2) True)
        (rewrite-with
          (lemma mod_hi)
          lr
          lhs
          ()
          ((steps ((compute both)) refl))
          refl)
        (steps () (by arith (list 2 1 1 1 0)))))))
(proof-for rt_row#0#decrease (by arith (list)))
(proof-for rt_row#0#nonneg (by arith (list 1 1)))
(proof-for rt_rows#0#decrease (by arith (list)))
(proof-for rt_rows#0#nonneg (by arith (list 1 1)))
