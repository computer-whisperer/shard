;;; std/map — back-compat shim. `map` is now a directory-module: std/map/ holds
;;; the interface mod.req.shard (opaque Map type + empty/lookup/insert + the
;;; extensional lemmas) and map.shard (private ctors + bodies + proofs). Proofs
;;; get the opaque interface; a RUN loads the bodies.
(import "map")
