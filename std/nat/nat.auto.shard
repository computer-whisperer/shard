;;; proof sidecar — MACHINE-OWNED, written by tools/prove (comments are
;;; not preserved). check REPLAYS these entries; it never searches.

(proof-for int_of_nat_succ (steps ((compute both)) refl))
(proof-for int_of_nat_zero (steps ((compute both)) refl))
(proof-for
  int_of_nat_nonneg
  (induct n
    ((case Z (steps ((compute both)) refl))
     (case S
       (c0)
       (steps
         ((simp both))
         (have (= (le 0 (int_of_nat c0)) True)
           (steps ((rewrite (hyp 0) lr lhs true ())) refl)
           (by arith (list 1 1))))))))
