;;; proof sidecar — MACHINE-OWNED, written by tools/prove (comments are
;;; not preserved). check REPLAYS these entries; it never searches.

(proof-for
  mod_10_lo
  (have (= (lt (mod n 10) 10) True)
    (rewrite-with (lemma mod_hi) lr lhs () ((steps ((compute both)) refl)) refl)
    (have (= (le 0 (mod n 10)) True)
      (rewrite-with
        (lemma mod_lo)
        lr
        lhs
        ()
        ((steps ((compute both)) refl))
        refl)
      (by arith (list 1 0 1)))))
(proof-for
  mod_10_hi
  (have (= (le 0 (mod n 10)) True)
    (rewrite-with (lemma mod_10_lo) lr lhs () () refl)
    (have (= (lt (mod n 10) 10) True)
      (rewrite-with
        (lemma mod_hi)
        lr
        lhs
        ()
        ((steps ((compute both)) refl))
        refl)
      (by arith (list 1 0 1)))))
