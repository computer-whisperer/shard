;;; proof sidecar — MACHINE-OWNED, written by tools/prove (comments are
;;; not preserved). check REPLAYS these entries; it never searches.

(proof-for idx_inner_simp (by arith (list)))
(proof-for sub_zero (by arith (list)))
(proof-for sub_sub_one (by arith (list)))
(proof-for reassoc_succ (by arith (list)))
(proof-for idx_cancel (by arith (list)))
(proof-for cap_idx (by arith (list)))
(proof-for idx_pred (by arith (list)))
