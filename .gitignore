/target
/rust_bootstrap/target
/shard-viewer/target

# Zed dev extension local grammar cache.
tools/zed-narrow/grammars/

/.claude

# Temporary compiler chain: derived artifacts (bin/rebuild.sh regenerates).
*.low.shard
*.low.shard.c
/bin/shard_eval
/bin/shard_check
/bin/*.stamp
.shard-cache/


# Emitted native artifacts (the *_write.shard emitters and the silicon
# differentials regenerate them; never tracked).
/examples/sha256sum/sha256sum
/examples/sha256sum/sha256sum_oneshot
/examples/sha256sum/sha256sum_shani
/examples/sha256sum/sha256sum_stream
*.bin
