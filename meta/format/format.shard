;;; meta/format — impl entry. This module is pure vocabulary: the formatter
;;; core (CST reader, printer, fmt, and the parse-equality gate) all lives in
;;; the interface. This file is the run-mode entry the bare-module import
;;; resolves to; it re-imports the interface so the functions load.

(import "mod.req.shard")
