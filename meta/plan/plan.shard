;;; meta/plan — impl entry. This module is pure vocabulary: everything lives
;;; in the interface (transparent types + the Target record and its law
;;; family). This file is the run-mode entry the bare-module import resolves
;;; to; it re-imports the interface so the types and ctors load.

(import "mod.req.shard")
