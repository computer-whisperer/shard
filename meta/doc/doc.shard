;;; meta/doc — impl entry. Pure vocabulary: everything lives in the
;;; interface. This file is the run-mode entry the bare-module import
;;; resolves to; it re-imports the interface so the definitions load.

(import "mod.req.shard")
