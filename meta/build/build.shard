;;; meta/build — impl entry. Pure vocabulary: everything lives in the
;;; interface (the transparent BuildProfile record + its law family).
;;; This file is the run-mode entry the bare-module import resolves to;
;;; it re-imports the interface so the types and ctors load.

(import "mod.req.shard")
