;;; statement_sugar_rejects.shard — MUST-FAIL: (inline NAME) requires NAME to
;;; be a nullary fn defined in THIS file; an unknown name is refused at
;;; expansion (None → the parse-failure path, named by the sugar diagnosis).
;;; Silent acceptance here would leave a call-shaped head in a claim spelling
;;; — exactly the drift the sugar exists to prevent.

(import "../kernel/stdlib.shard")

(fn broken () Int (inline no_such_fn))
